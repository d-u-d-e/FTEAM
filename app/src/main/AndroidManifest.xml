<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.es.findsoccerplayers">
    <!--
            The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
            Google Maps Android API v2, but you must specify either coarse or fine
            location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">
        <activity
            android:name=".ActivitySetLocation"
            android:parentActivityName=".ActivitySettings"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ActivityMaps"
            android:parentActivityName=".ActivityCreateMatch" />
        <activity
            android:name=".ActivityCreateMatch"
            android:parentActivityName=".ActivityMain" />
        <activity
            android:name=".ActivityBookedMatch"
            android:parentActivityName=".ActivityMain" />
        <activity
            android:name=".ActivityEditMatch"
            android:parentActivityName=".ActivityMain" />
        <activity
            android:name=".ActivityAccount"
            android:parentActivityName=".ActivitySettings" />
        <activity
            android:name=".ActivityInfoMatch"
            android:parentActivityName=".ActivityMain" />
        <activity
            android:name=".ActivitySettings"
            android:parentActivityName=".ActivityMain" />
        <activity
            android:name=".ActivityResetPassword"
            android:parentActivityName=".ActivityLogin"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ActivityRegister"
            android:parentActivityName=".ActivityLogin"
            android:screenOrientation="portrait" />
        <activity android:name=".ActivityMain"
            android:launchMode="singleTask" />
        <activity
            android:name=".ActivityLogin"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>